version: "3.5"

services:
  home_assistant:
    environment:
      # This is only an example and should get updated to match the service at hand.
      # Where to find Matierial Design Icon names: https://materialdesignicons.com/
      - SIDEBAR_DUPLICACY_GUI=        duplicacy_gui|duplicacy_gui.$DOMAIN|mdi:home-automation
